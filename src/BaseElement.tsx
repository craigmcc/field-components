// BaseElement ---------------------------------------------------------------

// If there was such a thing, this would be the default "element" component
// for the various input types.  In the meantime, it is the host for the
// Props interface for all standard elements, for consistency of overall typing.

// External Modules ----------------------------------------------------------

// Internal Modules ----------------------------------------------------------

import { BaseElementProps } from "./Types";

// Property Details ----------------------------------------------------------

export interface Props extends BaseElementProps {
}

